---
- name: Prepare
  hosts: all
  
  tasks:

    - name: Set Python3 for bullseye
      set_fact:
        ansible_python_interpreter: /usr/bin/python3
      when: ansible_distribution_release == 'bullseye'